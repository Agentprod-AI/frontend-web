import { Linkedin, Phone, Link, Twitter, Facebook } from "lucide-react";
import { Avatar, AvatarFallback, AvatarImage } from "./ui/avatar";
import { Organization } from "./layout/context/lead-user";

export const CompanyProfileSheet = (data: Organization) => {
  return <div>company profile</div>;
};
